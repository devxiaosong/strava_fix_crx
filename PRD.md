# 产品需求文档（PRD）
## Strava Bulk Edit Extension - Strava 批量编辑插件

**文档版本**: v1.0  
**创建日期**: 2025-01-06  
**产品负责人**: Stone  
**状态**: ✅ MVP 已完成，规划未来迭代

---

## 📋 目录

- [竞品功能分析](#竞品功能分析)
- [产品概述](#产品概述)
- [核心功能对比](#核心功能对比)
- [技术实现对比](#技术实现对比)
- [产品优势](#产品优势)

---

## 🔍 竞品功能分析

> **注：以下是对现有竞品 `updateActivities.js` 的详细分析**

### 竞品概述

**产品形式**: 原生 JavaScript Content Script  
**技术栈**: Vanilla JS + DOM 操作  
**代码规模**: 311 行纯 JavaScript  
**UI 框架**: 无（纯手工 DOM 构建）

### 竞品功能清单

#### ✅ 已实现功能

| 功能编号 | 功能名称 | 实现方式 | 代码行数 |
|---------|---------|---------|---------|
| C-F1 | UI 面板注入 | 手动 createElement | 3-209 行 |
| C-F2 | 批量更新骑行类型 | DOM 自动化 | 243-248 行 |
| C-F3 | 批量更换自行车 | DOM 自动化 | 250-255 行 |
| C-F4 | 批量更换跑鞋 | DOM 自动化 | 264-269 行 |
| C-F5 | 批量设置隐私 | DOM 自动化 | 271-276 行 |
| C-F6 | 自动翻页 | 递归调用 | 283-293 行 |
| C-F7 | 加载动画 | 自定义模态框 | 172-208 行 |

#### ❌ 未实现功能

- ❌ 进度显示（只有简单的 "正在更新..." 文字）
- ❌ 错误处理和重试机制
- ❌ 操作撤销功能
- ❌ 数据验证和确认
- ❌ 跑步类型编辑（代码中已注释掉，78-99 行）

---

### 详细功能分解

#### 1. UI 构建方式（第 3-170 行）

**实现特点**:
```javascript
// 竞品采用纯 DOM 操作构建 UI
const editPanel = document.createElement('div');
editPanel.classList.add('panel', 'panel-default');
filterPanel.insertAdjacentElement('afterend', editPanel);

// 逐个创建元素
const editPanelHeading = document.createElement('div');
editPanelHeading.classList.add('panel-heading');
editPanel.appendChild(editPanelHeading);

// ... 重复 200+ 行类似代码
```

**优点**:
- ✅ 无框架依赖，体积小
- ✅ 兼容性好
- ✅ 加载速度快

**缺点**:
- ❌ 代码冗长（200+ 行仅用于构建 UI）
- ❌ 难以维护和扩展
- ❌ 没有组件化思想
- ❌ 样式硬编码在 JS 中
- ❌ 代码可读性差

**代码示例**:
```javascript
// 创建一个下拉选择框需要 10+ 行代码
const editPanelRideTypeField = document.createElement('select');
editPanelRideTypeField.id = 'strava-bulk-edit-ride-type';
editPanelRideTypeField.classList.add('form-control');
editPanelRideTypeField.innerHTML = workoutTypeRideOptions.innerHTML.replace(
  /<option value="">All Ride Types/m, 
  "<option value=\"10\">Ride"
);
editPanelFormGroupRideType.appendChild(editPanelRideTypeField);
```

---

#### 2. 选项数据获取（第 41-114 行）

**实现方式**:
```javascript
// 从 Strava 页面的现有表单中复制选项
const workoutTypeRideOptions = document.getElementById('workout_type_ride');
editPanelRideTypeField.innerHTML = workoutTypeRideOptions.innerHTML.replace(
  /<option value="">All Ride Types/m, 
  "<option value=\"10\">Ride"
);
```

**优点**:
- ✅ 动态获取，与 Strava 保持同步
- ✅ 无需硬编码选项值

**缺点**:
- ⚠️ 使用字符串替换（不安全）
- ⚠️ 没有错误处理
- ⚠️ 魔法数字（value="10"）没有注释

---

#### 3. 核心更新逻辑（第 237-294 行）

**算法流程**:
```javascript
function updateActivities(fields) {
  // 步骤 1: 点击所有快速编辑按钮
  const quickEdit = document.querySelectorAll(
    '.training-activity-row .quick-edit'
  );
  for (let editButton of quickEdit) {
    editButton.click();
  }

  // 步骤 2: 填充表单字段
  if (fields.rideType) {
    const rideType = document.querySelectorAll(
      '.training-activity-row select[name="workout_type_ride"]'
    );
    for (let rideTypeField of rideType) {
      rideTypeField.value = fields.rideType;
    }
  }

  // 步骤 3: 提交所有表单
  const submit = document.querySelectorAll(
    '.training-activity-row button[type="submit"]'
  );
  for (let submitButton of submit) {
    submitButton.click();
  }

  // 步骤 4: 自动翻页
  const nextButton = document.querySelector('button.next_page');
  if (nextButton != null) {
    nextButton.click();
    setTimeout(updateActivities, 3000, fields); // 递归调用
  } else if (previousButton != null) {
    navigateBack(); // 返回第一页
  } else {
    toggleLoadingOverlay(); // 完成
  }
}
```

**优点**:
- ✅ 逻辑清晰，易懂
- ✅ 自动翻页处理完整
- ✅ 使用 setTimeout 避免过快操作

**缺点**:
- ❌ 硬编码延迟时间（3000ms）
- ❌ 没有进度反馈
- ❌ 没有错误处理
- ❌ 递归可能导致栈溢出（大量页面时）
- ❌ 没有中断机制
- ❌ 所有操作串行执行（较慢）

---

#### 4. 加载动画实现（第 172-208 行）

**实现方式**:
```javascript
// 创建模态框
const loadingModal = document.createElement('div');
loadingModal.id = 'strava-bulk-edit-loading-modal';
loadingModal.classList.add('modal', 'fade');
document.body.appendChild(loadingModal);

// 添加 Strava Logo（base64 编码）
const loadingImage = document.createElement('img');
loadingImage.src = 'data:image/png;base64,iVBORw0KG...'; // 超长 base64 字符串
```

**优点**:
- ✅ 提供视觉反馈
- ✅ 使用 base64 嵌入图片（无需额外请求）

**缺点**:
- ❌ 只有静态文字提示
- ❌ 无进度条
- ❌ 无完成百分比
- ❌ 无预计剩余时间
- ❌ base64 字符串直接写在代码中（可读性差）

---

#### 5. 翻页处理（第 283-305 行）

**返回第一页的实现**:
```javascript
function navigateBack() {
  const previousButton = document.querySelector('button.previous_page');
  
  if (previousButton != null) {
    previousButton.click();
    setTimeout(navigateBack, 3000); // 递归返回
  } else {
    toggleLoadingOverlay(); // 到达第一页
  }
}
```

**优点**:
- ✅ 自动返回第一页
- ✅ 递归实现简洁

**缺点**:
- ❌ 固定延迟（3秒），可能太慢或太快
- ❌ 没有失败重试
- ❌ 递归深度不受控

---

### 竞品技术栈分析

#### 代码结构

```
updateActivities.js (311 行)
├── UI 构建 (200+ 行)
│   ├── 面板创建
│   ├── 表单字段
│   ├── 按钮
│   └── 模态框
├── 事件处理 (30 行)
│   └── 点击事件
├── 核心逻辑 (60 行)
│   ├── updateActivities
│   ├── navigateBack
│   └── toggleLoadingOverlay
└── 工具函数 (20 行)
```

#### 依赖分析

**外部依赖**: 无  
**浏览器 API**: 
- `document.querySelector/querySelectorAll`
- `createElement/appendChild`
- `setTimeout`
- `classList`

**Strava 页面依赖**:
- `.search .panel` - 搜索面板
- `.training-activity-row` - 活动行
- `.quick-edit` - 快速编辑按钮
- `#workout_type_ride` - 骑行类型选择器
- `#gear_bike` - 自行车选择器
- `#gear_shoe` - 跑鞋选择器
- `button.next_page` - 下一页按钮
- `button.previous_page` - 上一页按钮

---

### 竞品性能分析

#### 优点
- ✅ **启动快**: 无框架加载，直接执行
- ✅ **体积小**: 单文件 311 行，~15KB
- ✅ **内存占用低**: 无 React Virtual DOM

#### 缺点
- ❌ **UI 构建慢**: 200+ 行同步 DOM 操作
- ❌ **更新效率**: 固定 3 秒延迟，无优化
- ❌ **可扩展性差**: 添加新功能需要大量代码

---

### 竞品用户体验分析

#### 优点体验

1. **自动化程度高**
   - ✅ 点击一次即可完成所有操作
   - ✅ 自动翻页，无需手动干预

2. **UI 集成好**
   - ✅ 直接嵌入 Strava 页面
   - ✅ 使用 Strava 的样式类

#### 缺点体验

1. **反馈不足** ⚠️
   - ❌ 没有进度显示
   - ❌ 不知道处理了多少个活动
   - ❌ 不知道还需要多久完成
   - ❌ 用户只能等待

2. **错误处理差** ⚠️
   - ❌ 网络失败时没有提示
   - ❌ 没有重试机制
   - ❌ 操作失败用户不知道

3. **无法中断** ⚠️
   - ❌ 开始后无法取消
   - ❌ 发现错误也无法停止

4. **无撤销功能** ⚠️
   - ❌ 误操作无法恢复
   - ❌ 没有操作历史

---

### 竞品代码质量分析

#### 可维护性评分: ⭐⭐☆☆☆ (2/5)

**问题点**:
1. **无代码结构**: 所有代码在一个文件中
2. **无注释**: 只有一条跑步类型的注释
3. **魔法数字**: 3000, 1500 等延迟时间没有说明
4. **重复代码**: 大量重复的 DOM 操作
5. **无类型检查**: 纯 JS，容易出错

**优点**:
1. ✅ 代码简单直接
2. ✅ 易于理解流程

#### 可扩展性评分: ⭐☆☆☆☆ (1/5)

**限制**:
1. 添加新字段需要大量代码
2. 修改 UI 需要重写 DOM 操作
3. 无组件化，无法复用

#### 测试性评分: ⭐☆☆☆☆ (1/5)

**问题**:
1. ❌ 无单元测试
2. ❌ 难以 mock DOM 操作
3. ❌ 无法独立测试逻辑

---

### 竞品安全性分析

#### 潜在风险

1. **字符串注入风险** ⚠️
```javascript
// 使用正则替换 HTML，可能导致注入
.innerHTML = workoutTypeRideOptions.innerHTML.replace(
  /<option value="">All Ride Types/m, 
  "<option value=\"10\">Ride"
);
```

2. **无输入验证** ⚠️
```javascript
// 直接设置值，没有验证
rideTypeField.value = fields.rideType;
```

3. **无错误边界** ⚠️
- 任何步骤失败会导致整个流程中断
- 用户看不到错误信息

4. **误操作风险** ⚠️
- 没有二次确认
- 没有预览功能
- 无法撤销

---

## 💡 从竞品中学到的经验

### 值得保留的设计

1. ✅ **自动翻页机制**
   - 递归处理所有页面
   - 完成后自动返回第一页
   - **我们的改进**: 添加进度显示和错误处理

2. ✅ **从页面获取选项**
   - 动态读取 Strava 的选项
   - 无需硬编码
   - **我们的改进**: 使用 React state 管理，更安全

3. ✅ **延迟处理**
   - 使用 setTimeout 避免触发限流
   - **我们的改进**: 智能延迟，根据响应时间调整

### 需要改进的设计

1. ❌ **UI 构建方式**
   - **问题**: 200+ 行手工 DOM 操作
   - **我们的方案**: React 组件化，20 行解决

2. ❌ **进度反馈**
   - **问题**: 只有 "正在更新..." 文字
   - **我们的方案**: 实时进度条 + 百分比 + 数量

3. ❌ **错误处理**
   - **问题**: 无错误处理
   - **我们的方案**: try-catch + 用户提示 + 重试

4. ❌ **代码组织**
   - **问题**: 单文件 311 行
   - **我们的方案**: 模块化 + 组件化

---

## 🎬 总结

### 竞品优点
- ✅ 核心功能完整
- ✅ 自动化程度高
- ✅ 无框架依赖（轻量）

### 竞品缺点
- ❌ 代码质量低
- ❌ 用户体验差
- ❌ 难以维护和扩展
- ❌ 无错误处理
- ❌ 无进度反馈

---

## 📝 竞品用户反馈收集

> **注：以下是真实用户对竞品的反馈和功能需求**

### 用户反馈原文

1. **选择性更新功能**
   - 勾选标记来选择要更改的内容。

2. **筛选功能增强**
   - 也无法筛选出步行活动中使用过的鞋子。
   - 在筛选条件中添加日期范围也会非常有用。

3. **隐藏开始时间功能**
   - Can we make the new feature hidden start time in bulk edit?

4. **更新确认机制**
   - Confirm the update. Show the amount of activities that will be updated before actually updating.

5. **文档和开发说明**
   - Add a wiki page with development instructions？(买个网址)

6. **性能优化建议**
   - 脚本会获取所有活动，然后编辑隐私设置，最后保存活动。点击"下一页"按钮后，脚本会等待 3 秒，直到下一页的活动加载完毕。如果活动加载完毕后脚本能够继续执行，而不是设置固定的超时时间，那就更好了。

7. **字段选择功能**
   - 如果能够选择要更新的字段，那就方便多了。例如，可以在可用字段前添加一个复选框。这样，您就可以一次性勾选要更新的字段前的复选框。未勾选复选框的字段将不会被更新。

---

## 💰 商业模式设计

### 设计理念

通过**场景化 + 能力分层**的方式，实现用户体验与商业价值的平衡：

- **底层架构**：统一强大的规则引擎，支持最复杂的场景
- **场景配置**：通过配置控制开放能力，实现能力分层
- **商业策略**：简单场景免费（用户获取），复杂规则付费（价值变现）

---

### 能力分层策略

#### 免费层：简单场景

**开放场景**：
- 🔒 **调整隐私设置**
  - 单次配置模式
  - 基础匹配条件：运动类型 + 时间范围
  - 一键批量修改隐私设置
  
- 🏃 **修改运动类型**
  - 单次配置模式
  - 基础匹配条件：原类型 + 时间 + 距离
  - 快速更正分类错误

**设计考量**：
- ✅ **用户体验**：零门槛，易上手，满足 80% 用户的基本需求
- ✅ **用户获取**：足够实用，建立用户基础和口碑
- ✅ **价值展示**：让用户体验到批量操作的价值

---

#### 付费层：复杂场景（增值服务）

**开放场景**：
- 👟 **更新跑鞋记录** 🔒 专业版
  - 规则列表模式（支持多条规则）
  - 高级匹配条件：时间 + 距离 + 跑步类型 + 时长
  - 精准记录装备使用历史
  - 支持规则模板保存和复用
  
- 🚴 **更新骑行装备** 🔒 专业版
  - 规则列表模式（支持多条规则）
  - 高级匹配条件：时间 + 距离 + 骑行类型 + 时长
  - 记录车辆更换历史
  - 支持规则模板保存和复用

**设计考量**：
- ✅ **真实痛点**：专业运动员、数据控需要精准的装备记录
- ✅ **价值主张**：节省数百小时手工填写时间
- ✅ **付费意愿**：解决刚需，用户愿意为此付费
- ✅ **差异化**：竞品不具备的高级能力

---

### 技术实现

#### 统一的底层架构

```
通用规则引擎（一次开发，支持所有场景）
  ↓
场景配置文件（控制开放能力）
  ↓
用户权限系统（免费 vs 付费）
```

#### 场景配置示例

**免费场景**（调整隐私）：
```json
{
  "id": "update-privacy",
  "requiresPro": false,
  "ui": {
    "mode": "single",
    "matchRuleFields": [
      { "type": "activityType" },
      { "type": "dateRange" }
    ]
  }
}
```

**付费场景**（更新跑鞋）：
```json
{
  "id": "update-running-shoes",
  "requiresPro": true,
  "proFeature": "多规则配置 + 高级匹配条件",
  "ui": {
    "mode": "rule-list",
    "matchRuleFields": [
      { "type": "dateRange" },
      { "type": "distance" },
      { "type": "duration" },
      { "type": "workoutType" }
    ]
  }
}
```

---

### 用户界面展示

#### 场景选择界面（含付费标识）

```
┌─────────────────────────────────────┐
│ 选择要执行的操作：                    │
│                                     │
│ 🔒 调整隐私设置      [选择] →        │
│    💚 免费                          │
│                                     │
│ 👟 更新跑鞋记录      [🔒 升级]       │
│    ⭐ 专业版功能                     │
│                                     │
│ 🚴 更新骑行装备      [🔒 升级]       │
│    ⭐ 专业版功能                     │
│                                     │
│ 🏃 修改运动类型      [选择] →        │
│    💚 免费                          │
└─────────────────────────────────────┘
```

#### 付费场景点击后的提示

```
┌─────────────────────────────────────┐
│ 👟 更新跑鞋记录（专业版功能）         │
├─────────────────────────────────────┤
│                                     │
│ 此功能需要升级到专业版：              │
│                                     │
│ ✨ 支持多条规则配置                  │
│ ✨ 按时间段 + 距离精准匹配            │
│ ✨ 完整记录装备使用历史               │
│ ✨ 规则模板保存和复用                │
│                                     │
│ 💡 专业运动员的选择                  │
│    节省数百小时手工填写时间           │
│                                     │
│ [了解专业版] [暂不升级]               │
└─────────────────────────────────────┘
```

---

### 定价策略（参考）

#### 方案 A：订阅制

```
免费版：
- 🔒 调整隐私设置
- 🏃 修改运动类型
- 基础匹配条件

专业版 - $4.99/月 或 $49.99/年：
- ✅ 免费版所有功能
- 👟 更新跑鞋记录（多规则）
- 🚴 更新骑行装备（多规则）
- 规则模板保存和复用
- 高级匹配条件
- 优先技术支持

终身版 - $99.99：
- ✅ 专业版所有功能
- 永久使用
- 未来新场景优先体验
```

#### 方案 B：按场景付费

```
免费版：
- 基础场景永久免费

单场景解锁 - $9.99/场景：
- 👟 更新跑鞋记录 - $9.99（一次性）
- 🚴 更新骑行装备 - $9.99（一次性）

全场景包 - $19.99：
- 所有专业场景永久使用
- 未来新场景自动解锁
```

---

### 价值主张

#### 免费层
**Slogan**: "批量操作，节省时间"

**目标用户**：
- 普通 Strava 用户
- 偶尔需要批量修改隐私的用户
- 需要更正活动分类的用户

**核心价值**：
- 快速批量修改，节省时间
- 零学习成本，简单易用
- 完全免费，无需付费

---

#### 付费层
**Slogan**: "专业运动员的装备管理工具"

**目标用户**：
- 专业/半专业运动员
- 数据控、装备党
- 需要精准记录装备使用历史的用户

**核心价值**：
- **节省时间**：自动化填写，节省数百小时
- **数据精准**：按时间段、距离精准匹配装备
- **历史回顾**：完整的装备使用历史记录
- **专业工具**：竞品不具备的高级能力

**价值量化**：
```
假设：
- 一年跑步 200 次
- 换鞋 3 次
- 手工填写每次 30 秒

手工填写总时间：200 × 30秒 = 100 分钟/年
使用工具：配置 3 条规则，总计 5 分钟

节省时间：95 分钟/年
年费 $49.99 ≈ 每分钟 $0.5

物有所值！
```

---

### 核心优势

#### 1. 用户体验优先
- 免费功能足够实用，不是"阉割版"
- 付费功能解决真实痛点，不是"强制付费"
- 自然的升级路径，不骚扰用户

#### 2. 商业价值清晰
- 免费层：用户获取和口碑传播
- 付费层：解决刚需，付费意愿强
- 长期价值：订阅收入稳定

#### 3. 技术架构统一
- 一套底层引擎，支持所有场景
- 通过配置控制能力开放
- 易于扩展新的付费场景

#### 4. 竞争壁垒
- 场景化设计降低学习成本
- 规则引擎支持复杂需求
- 配置化系统快速迭代

---

### V1.0 实现范围

**免费场景**：
- ✅ 调整隐私设置
- ✅ 修改运动类型

**付费场景**：
- ✅ 更新跑鞋记录
- ✅ 更新骑行装备

**权限系统**：
- ✅ 场景配置中的权限标识
- ✅ 付费场景的锁定和升级提示
- ⚠️ 实际付费流程（V1.0 暂不实现，可以通过激活码方式）

**V1.0 策略**：
- 先实现所有场景的完整功能
- 用权限配置区分免费/付费
- 初期可通过激活码解锁（测试市场反应）
- V2.0 再接入真实的支付系统

---

## 🎯 V1.0 功能框架（第一步目标）

### 目标定位
**覆盖竞品功能 + 关键体验优化**（不增加新的更新字段）

---

### 核心功能（竞品已有）

#### F1. 批量更新字段 🔄 待开发
- 骑行类型 (Ride Type)
- 自行车 (Bike)
- 跑鞋 (Shoes)
- 隐私设置 (Privacy)

#### F2. 自动翻页处理 🔄 待开发
- 自动检测下一页
- 递归处理所有页面
- 完成后返回第一页

#### F3. 基础进度显示 🔄 待开发
- 加载模态框
- 实时进度计数
- 进度条

---

### 体验优化功能（新增）

#### F4. 更新前确认 🔄 待开发
**来源**: 用户反馈 #4
- 显示将要更新的活动数量
- 显示选择的更新字段
- 用户二次确认后执行

#### F5. 字段选择优化 🔄 待开发
**来源**: 用户反馈 #7 + 竞品缺陷
- 可选择是否更新某个字段
- 未选择的字段保持原值不变
- 避免误操作覆盖

#### F6. 智能等待机制 🔄 待开发
**来源**: 用户反馈 #6 + 竞品缺陷
- 检测页面加载状态（代替固定 3 秒）
- 动态调整等待时间
- 提升整体执行速度

#### F7. 错误处理增强 🔄 待开发
**来源**: 竞品缺陷
- 网络错误提示
- 失败活动记录
- 部分成功也给出反馈

#### F8. 操作中断功能 🔄 待开发
**来源**: 竞品缺陷
- 提供取消按钮
- 可中止正在进行的更新
- 已更新的不回滚

#### F9. 日期范围筛选 🔄 待开发
**来源**: 用户反馈 #2
- 添加日期范围选择器
- 仅对指定日期范围内的活动生效
- 支持常用快捷选项（最近 7 天、30 天、3 个月等）

---

### 功能优先级

```
P0 - 必须实现（共4个功能，预计 12 天）
├── 🔄 F1: 批量更新字段（5 天）
├── 🔄 F2: 自动翻页处理（2 天）
├── 🔄 F3: 基础进度显示（3 天）
└── 🔄 F4: 更新前确认（2 天）

P1 - 应该实现（共4个功能，预计 10 天）
├── 🔄 F5: 字段选择优化（3 天）
├── 🔄 F6: 智能等待机制（2 天）
├── 🔄 F7: 错误处理增强（2 天）
└── 🔄 F9: 日期范围筛选（3 天）

P2 - 可以实现（共1个功能，预计 3 天）
└── 🔄 F8: 操作中断功能（3 天）

总计：9个功能，预计 25 天
```

---

### 不在 V1.0 范围内的功能

**暂不实现**（留待后续版本）:
- ❌ 新增更新字段（如：活动名称、描述、标签等）
- ❌ 步行活动的鞋子筛选
- ❌ 隐藏开始时间功能
- ❌ 撤销功能
- ❌ 操作历史记录
- ❌ Wiki 文档站点

**原因**: 第一步专注于完善核心体验，不过度扩展功能边界

---

## 🔄 用户操作流程设计

### 🎯 设计理念：场景化架构

#### 核心思想
用户不是在想"我要修改什么字段"，而是在想**"我要做什么事"**。

```
传统设计：选字段 → 设条件 → 填值
场景设计：选场景 → 场景自动匹配合理的字段和条件
         ↓
      更符合心智模型
```

#### 三层架构

```
┌─────────────────────────────────────────────────┐
│  📱 表现层：场景化入口（UI）                      │
│  • 用户看到：👟更新跑鞋、🔒调整隐私...            │
│  • 零学习成本，选场景即可                        │
└─────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────┐
│  🧩 配置层：场景定义（JSON Config）              │
│  • 场景 ID、名称、图标                           │
│  • UI 模板定义                                  │
│  • 匹配规则模板                                 │
│  • 更新值模板                                   │
│  • ⚡ 可在线更新，无需发版                       │
└─────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────┐
│  ⚙️ 执行层：通用规则引擎（Core Engine）          │
│  • 支持最复杂的规则组合                         │
│  • 1个任务 → N个更新规则                        │
│  • 1个更新规则 → N个匹配规则 + 更新值            │
└─────────────────────────────────────────────────┘
```

#### 数据结构层次

```
Task (任务 - 用户一次操作)
  ├─ scenarioId: string           // 关联的场景ID
  └─ updateRules: UpdateRule[]    // 多个更新规则
       ├─ name: string            // 规则描述
       ├─ matchRules: MatchRule[] // 多个匹配条件（AND关系）
       │    ├─ type: 'activityType' | 'dateRange' | 'distance' | ...
       │    ├─ operator: 'eq' | 'gt' | 'lt' | 'between' | 'in'
       │    └─ value: any
       └─ updateValues: object    // 要更新的字段值
```

**关键特性**：
- ✅ 底层支持最复杂的场景（多规则、多条件）
- ✅ 场景化入口简化用户学习成本
- ✅ 场景定义可配置，新增场景无需发版

---

### 整体流程概览

```
用户访问 Strava 训练页面
    ↓
看到"批量更新"按钮
    ↓
点击按钮 → 选择场景
    ↓
根据场景显示对应的配置界面
    ↓
试运行（可选）
    ↓
查看影响预览
    ↓
正式运行
    ↓
查看运行结果
```

---

## 📱 场景化操作流程

### 步骤 1: 场景选择入口

**位置**: Strava 训练日志页面的搜索筛选面板附近  
**触发**: 插件自动在页面植入 **"批量更新"** 按钮

**点击后显示场景选择界面**:

```
┌─────────────────────────────────────┐
│ 选择要执行的操作：                    │
│                                     │
│ 🔒 调整隐私设置      [选择] →        │
│                                     │
│ 👟 更新跑鞋记录      [选择] →        │
│                                     │
│ 🚴 更新骑行装备      [选择] →        │
│                                     │
│ 🏃 修改运动类型      [选择] →        │
└─────────────────────────────────────┘
```

**说明**：
- 场景列表从配置文件动态加载
- 配置源：https://yoursite.com/strava-scenarios.json
- 支持缓存，离线可用最近一次的配置

---

### 场景 A: 调整隐私设置（简单场景）

**适用场景**: 批量修改活动的可见性设置

#### 界面设计

```
┌─────────────────────────────────────┐
│ 🔒 批量调整隐私设置                   │
├─────────────────────────────────────┤
│ 1️⃣ 筛选要修改的活动：                │
│                                     │
│ 运动类型（可选）：                    │
│  [下拉多选: 骑行, 跑步 ▼]           │
│  💡 默认全选，至少选择一个            │
│                                     │
│ 时间范围（可选）：                    │
│  ┌────────────────────────────────┐ │
│  │ 📅 2024/01/01 - 2024/02/28     │ │
│  │                     [删除]      │ │
│  └────────────────────────────────┘ │
│  ┌────────────────────────────────┐ │
│  │ 📅 2024/06/01 - 2024/06/30     │ │
│  │                     [删除]      │ │
│  └────────────────────────────────┘ │
│  [+ 添加时间段]                     │
│                                     │
├─────────────────────────────────────┤
│ 2️⃣ 修改为：                          │
│                                     │
│ 隐私设置：[下拉: 仅关注者 ▼] *必填   │
│  • 所有人                           │
│  • 仅关注者                         │
│  • 仅自己                           │
│                                     │
├─────────────────────────────────────┤
│ [取消] [预览匹配结果] [直接执行]      │
└─────────────────────────────────────┘
```

**特点**：
- ✅ 单次配置模式（无需规则列表）
- ✅ 运动类型可多选
- ✅ 时间范围可选（支持快捷选择）
- ✅ 修改值简单（只有隐私设置）

---

### 场景 B: 更新跑鞋记录（复杂场景）

**适用场景**: 
- 记录不同时期更换的跑鞋
- 根据距离区分训练鞋/比赛鞋
- 不同跑步类型使用不同的鞋

#### 界面设计

```
┌─────────────────────────────────────────────────────────┐
│ 👟 批量更新跑鞋记录                                        │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ 📋 更新规则列表（按顺序执行）                             │
│                                                         │
│ ┌───────────────────────────────────────────────────┐   │
│ │ 规则 1 ✏️                                          │   │
│ │ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │   │
│ │ 时间段：2024/01/01 - 2024/02/28                   │   │
│ │ 距离：  > 10 km                                   │   │
│ │ 类型：  [x] Long Run  [x] Workout                 │   │
│ │ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │   │
│ │ 更新为：Hoka Clifton 9                            │   │
│ │ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │   │
│ │                                                   │   │
│ │ [查看详情] [编辑] [删除] [复制]                    │   │
│ └───────────────────────────────────────────────────┘   │
│                                                         │
│ ┌───────────────────────────────────────────────────┐   │
│ │ 规则 2 ✏️                                          │   │
│ │ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │   │
│ │ 时间段：2024/01/01 - 2024/02/28                   │   │
│ │ 距离：  < 10 km                                   │   │
│ │ 类型：  [ ] Long Run  [x] Workout  [x] Race       │   │
│ │ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │   │
│ │ 更新为：Nike Pegasus 40                           │   │
│ │ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │   │
│ │                                                   │   │
│ │ [查看详情] [编辑] [删除] [复制]                    │   │
│ └───────────────────────────────────────────────────┘   │
│                                                         │
│ [+ 添加新规则]                                          │
│                                                         │
├─────────────────────────────────────────────────────────┤
│ ⚠️  如果有活动匹配多条规则，后执行的规则会覆盖前面的         │
│                                                         │
│ [预览所有匹配的活动] [执行所有规则] [保存为模板]           │
└─────────────────────────────────────────────────────────┘
```

#### 添加/编辑规则弹窗

```
┌─────────────────────────────────────────────────┐
│ ✏️ 编辑规则 #2                                   │
├─────────────────────────────────────────────────┤
│                                                 │
│ 1️⃣ 筛选条件                                      │
│                                                 │
│ 时间范围：*必填                                   │
│  起始：[📅 2024/01/01]                          │
│  结束：[📅 2024/02/28]                          │
│                                                 │
│ 距离范围：                                       │
│  ( ) 不限                                       │
│  ( ) 小于  [10] km                              │
│  (•) 大于  [10] km                              │
│  ( ) 区间  [___] ~ [___] km                    │
│                                                 │
│ 时长范围：                                       │
│  (•) 不限                                       │
│  ( ) 小于  [___] 分钟                           │
│  ( ) 大于  [___] 分钟                           │
│  ( ) 区间  [___] ~ [___] 分钟                  │
│                                                 │
│ 跑步类型：                                       │
│  [ ] Race                                       │
│  [x] Workout                                    │
│  [x] Long Run                                   │
│  💡 不选表示匹配所有类型                         │
│                                                 │
├─────────────────────────────────────────────────┤
│ 2️⃣ 更新值                                        │
│                                                 │
│ 跑鞋：*必填                                      │
│  [下拉: Nike Pegasus 40 ▼]                      │
│                                                 │
│ 同时修改（可选）：                                │
│  [x] 跑步类型：[下拉: Long Run ▼]                │
│  [ ] 隐私设置：[下拉: 仅关注者 ▼]                 │
│                                                 │
├─────────────────────────────────────────────────┤
│ [取消] [预览匹配结果] [保存规则]                  │
└─────────────────────────────────────────────────┘
```

**特点**：
- ✅ 规则列表模式（支持多条规则）
- ✅ 每条规则独立的筛选条件和更新值
- ✅ 运动类型固定为"跑步"（场景自动设置）
- ✅ 跑鞋为必填项
- ✅ 可附带修改跑步类型、隐私等
- ✅ 支持规则复制（快速创建相似规则）

**使用示例**：
```
规则1：2024/01-02月 + 距离>10km → Hoka Clifton 9（长距离鞋）
规则2：2024/01-02月 + 距离<10km → Nike Pegasus 40（训练鞋）
规则3：2024/03-04月 + 距离>15km → Asics Gel-Kayano 29（新买的长距离鞋）
规则4：2024/03-04月 + 距离<15km → Nike Pegasus 40（继续用）
规则5：2024/05月后 + Race类型 → Saucony Endorphin Pro（比赛鞋）
```

---

### 场景 C: 更新骑行装备（复杂场景）

**适用场景**: 记录不同时期更换的自行车

#### 界面设计（与跑鞋类似）

```
┌─────────────────────────────────────────────────────────┐
│ 🚴 批量更新骑行装备                                        │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ 📋 更新规则列表（按顺序执行）                             │
│                                                         │
│ [规则卡片展示，与跑鞋场景类似]                            │
│                                                         │
│ [+ 添加新规则]                                          │
│                                                         │
├─────────────────────────────────────────────────────────┤
│ 💡 总计将影响约 25 个活动                                │
│                                                         │
│ [预览所有匹配的活动] [执行所有规则] [保存为模板]           │
└─────────────────────────────────────────────────────────┘
```

**特点**：
- ✅ 运动类型固定为"骑行"
- ✅ 自行车为必填项
- ✅ 可附带修改骑行类型、隐私等
- ✅ 筛选条件包括：时间、距离、骑行类型等

---

### 场景 D: 修改运动类型（简单场景）

**适用场景**: 批量更正活动分类错误

#### 界面设计

```
┌─────────────────────────────────────┐
│ 🏃 批量修改运动类型                   │
├─────────────────────────────────────┤
│ 1️⃣ 筛选要修改的活动：                │
│                                     │
│ 原运动类型：[下拉: 骑行 ▼]           │
│                                     │
│ 时间范围（可选）：                    │
│  [📅 2024/03/01] - [📅 2024/03/31]  │
│                                     │
│ 距离范围（可选）：                    │
│  最小：[___] km   最大：[10] km     │
│                                     │
├─────────────────────────────────────┤
│ 2️⃣ 修改为：                          │
│                                     │
│ 新运动类型：[下拉: 虚拟骑行 ▼] *必填  │
│                                     │
│ ⚠️ 注意：修改运动类型后，原装备       │
│    设置可能不再适用                  │
│                                     │
├─────────────────────────────────────┤
│ [取消] [预览匹配结果] [直接执行]      │
└─────────────────────────────────────┘
```

**特点**：
- ✅ 单次配置模式
- ✅ 需要选择原运动类型和新运动类型
- ✅ 有警告提示（装备可能不适用）

---

## 🔄 通用执行流程

以下流程适用于所有场景：

### 步骤 1: 预览匹配结果（可选）

**触发**: 用户点击"预览匹配结果"按钮

**执行过程**:
```
显示进度对话框
    ↓
自动翻页遍历所有页面
    ↓
识别符合条件的活动
    ↓
模拟更新数据（不真实写入）
    ↓
记录每个活动的修改前后数据到内存
    ↓
显示预览结果
```

**预览结果对话框**:

```
┌──────────────────────────────────────┐
│  预览完成                             │
├──────────────────────────────────────┤
│  符合条件的活动：25 个                │
│  修改字段：跑鞋                       │
│                                      │
│  将要修改的数据预览：                 │
│  ┌────────────────────────────────┐ │
│  │ 规则1 匹配 12 个活动：          │ │
│  │ • 2024-01-15 早晨跑步           │ │
│  │   跑鞋: (无) → Hoka Clifton 9  │ │
│  │ • 2024-01-18 长距离训练         │ │
│  │   跑鞋: Nike Pegasus 40 → Hoka │ │
│  │ ... (展开查看全部)              │ │
│  │                                │ │
│  │ 规则2 匹配 18 个活动：          │ │
│  │ • 2024-01-16 速度训练           │ │
│  │   跑鞋: (无) → Nike Pegasus 40 │ │
│  │ ... (展开查看全部)              │ │
│  └────────────────────────────────┘ │
│                                      │
│  ⚠️  冲突提示：                      │
│  • 活动#12345678 匹配了2条规则       │
│    规则2 将覆盖规则1                 │
│                                      │
│  [返回修改] [取消] [开始正式执行]     │
└──────────────────────────────────────┘
```

**特点**：
- ✅ 按规则分组显示匹配结果
- ✅ 显示修改前后对比
- ✅ 高亮冲突的活动
- ✅ 所有数据可滚动查看
- ✅ 不真实写入数据

---

### 步骤 2: 正式执行

**触发**: 
- 用户点击"开始正式执行"按钮（预览后）
- 或点击"直接执行"按钮（跳过预览）

**执行过程**:
```
显示进度对话框
    ↓
按顺序执行每条规则
    ↓
  ├─ 自动翻页遍历所有页面
  ├─ 对符合条件的活动执行修改
  ├─ 自动提交保存
  └─ 记录成功和失败的活动
    ↓
显示运行结果
```

**进度显示**:

```
┌──────────────────────────────────────┐
│  正在执行批量更新...                  │
├──────────────────────────────────────┤
│                                      │
│  当前规则：规则2 / 共5条              │
│  规则描述：1-2月短距离跑              │
│                                      │
│  当前页面：第 3 / 总共 8 页           │
│  已更新活动：15 / 38 个               │
│                                      │
│  ✅ 成功：14 个                       │
│  ❌ 失败：1 个                        │
│                                      │
│  进度：[████████░░░░] 39%            │
│  预计剩余时间：约 2 分钟              │
│                                      │
│  [暂停]                              │
└──────────────────────────────────────┘
```

**运行结果对话框**:

```
┌──────────────────────────────────────┐
│  批量更新完成                         │
├──────────────────────────────────────┤
│  ✅ 成功更新：36 个活动               │
│  ❌ 失败：2 个活动                    │
│                                      │
│  执行详情：                           │
│  • 规则1：成功 12 个，失败 0 个       │
│  • 规则2：成功 17 个，失败 1 个       │
│  • 规则3：成功 7 个，失败 1 个        │
│                                      │
│  失败详情：                           │
│  • 活动#12345: 网络错误 (规则2)      │
│  • 活动#67890: 权限不足 (规则3)      │
│                                      │
│  总耗时：3 分 25 秒                   │
│                                      │
│  [查看详情] [关闭]                    │
└──────────────────────────────────────┘
```

---

### 步骤 3: 暂停/继续功能

**适用场景**: 预览/执行过程中

**暂停后显示**:

```
┌──────────────────────────────────────┐
│  已暂停                               │
├──────────────────────────────────────┤
│  当前规则：规则2 / 共5条              │
│  当前进度：已处理 15 / 38 个活动      │
│  成功：14 个，失败：1 个              │
│                                      │
│  对话框保持打开状态（遮罩页面）        │
│                                      │
│  [继续执行] [停止并返回]              │
└──────────────────────────────────────┘
```

**说明**:
- ✅ 点击暂停后，对话框保持打开（遮罩页面）
- ✅ 继续执行：从当前规则的当前页面继续处理
- ✅ 停止并返回：已修改的数据保留，不撤销
- ⚠️ V1.0 不实现撤销功能

---

## 🧩 配置化场景系统

### 场景配置结构

每个场景由 JSON 配置定义，包含：

```
ScenarioConfig
├─ id: string                    // 场景唯一标识
├─ version: string               // 配置版本号
├─ meta                          // 基础信息
│  ├─ name: string               // 场景名称
│  ├─ icon: string               // 图标
│  ├─ description: string        // 描述
│  ├─ category: string           // 分类
│  └─ complexity: string         // 复杂度
├─ ui                            // UI配置
│  ├─ mode: string               // 'single' | 'rule-list'
│  ├─ matchRuleFields: []        // 匹配规则字段配置
│  └─ updateValueFields: []      // 更新值字段配置
└─ validation                    // 验证规则
   ├─ requiredMatchRules: []     // 必须的匹配条件
   ├─ requiredUpdateValues: []   // 必须的更新值
   └─ conflictDetection: bool    // 是否检测冲突
```

### 配置加载流程

```
插件启动
    ↓
检查本地缓存
    ↓
请求远程配置
https://yoursite.com/strava-scenarios.json
    ↓
对比版本号
    ↓
更新本地缓存（如有新版本）
    ↓
渲染场景入口
```

### 配置示例：更新跑鞋场景

```json
{
  "id": "update-running-shoes",
  "version": "1.0.0",
  "meta": {
    "name": "更新跑鞋记录",
    "icon": "👟",
    "description": "记录不同时期、不同距离使用的跑鞋",
    "category": "gear",
    "complexity": "complex"
  },
  "ui": {
    "mode": "rule-list",
    "matchRuleFields": [
      {
        "type": "activityType",
        "label": "运动类型",
        "required": true,
        "widget": "select",
        "defaultValue": "Run",
        "readonly": true
      },
      {
        "type": "dateRange",
        "label": "时间范围",
        "required": true,
        "widget": "dateRange"
      },
      {
        "type": "distance",
        "label": "距离范围",
        "required": false,
        "widget": "numberRange",
        "options": {
          "unit": "km",
          "operators": ["gt", "lt", "between"]
        }
      }
    ],
    "updateValueFields": [
      {
        "field": "gear",
        "label": "跑鞋",
        "required": true,
        "widget": "gearSelect",
        "options": {
          "gearType": "shoes"
        }
      }
    ]
  },
  "validation": {
    "requiredMatchRules": ["activityType", "dateRange"],
    "requiredUpdateValues": ["gear"],
    "conflictDetection": true
  }
}
```

**优势**：
- ✅ 新增场景只需添加配置，无需发版
- ✅ 修改场景UI无需修改代码
- ✅ 支持A/B测试（不同配置版本）
- ✅ 快速响应用户需求

---

## ⚙️ 技术架构层次

### 执行引擎核心逻辑

**规则执行顺序**：
```
Task
  └─ UpdateRule[] (按顺序执行)
       ├─ 规则1
       │   ├─ 遍历所有页面
       │   ├─ 评估每个活动是否匹配所有 MatchRule（AND关系）
       │   ├─ 匹配则应用 UpdateValues
       │   └─ 记录结果
       ├─ 规则2
       │   └─ ...
       └─ 规则N
```

**冲突处理策略**：
- 后执行的规则覆盖前面的规则
- 预览时高亮冲突的活动
- 用户可调整规则顺序

**页面加载判断**：
- 方式1：监控 Strava API 接口返回
- 方式2：检测 DOM 中高亮的页码元素
- 方式3：结合两种方式，提高可靠性

**智能等待机制**：
- 检测页面加载状态（代替固定 3 秒）
- 动态调整等待时间
- 提升整体执行速度

---

### XPath 配置化：抗变化设计

#### 背景问题

插件需要高度耦合 Strava 的 DOM 结构来操作页面元素（点击按钮、填充表单等），但 Strava 可能随时改版，导致：

**问题链**：
```
Strava 改版
  ↓
DOM 结构变化（类名、ID、层级等变更）
  ↓
硬编码的选择器失效
  ↓
插件完全不可用
  ↓
必须：修改代码 → 打包 → 提交审核 → 等待通过（3-7天）→ 用户更新
  ↓
修复周期长，期间用户无法使用
```

#### 解决方案

**核心策略**：XPath 选择器配置化 + 云端实时更新

- 所有 DOM 选择器（XPath）不硬编码在代码中
- 存储在云端配置文件中
- 插件启动时自动下载最新配置
- 当 Strava 改版时，只需更新云端配置文件
- 用户无需更新插件，即时生效

**效果对比**：
```
传统方式：修复周期 3-7 天
配置化方式：修复周期 1-2 小时（发现问题 → 更新配置 → 用户自动获取）
```

---

#### 配置文件结构

**1. XPath 选择器配置文件（xpath-selectors.json）**

存储在云端：`https://yoursite.com/xpath-selectors.json`

包含内容：
- **版本号**：用于检测更新（如 2024.01.07.001）
- **更新时间**：配置文件的最后更新时间
- **选择器映射**：关键 DOM 元素的 XPath 表达式
  - 快速编辑按钮
  - 活动行
  - 各类表单字段（隐私、装备、类型等）
  - 提交按钮
  - 翻页按钮
  - 页面状态标识
- **备用选择器**：主选择器失败时的降级方案
- **兼容性信息**：适配的 Strava 版本、最低插件版本等

**选择器示例**：
- `quickEditButton`: 定位快速编辑按钮的 XPath
- `privacySelect`: 定位隐私设置下拉框的 XPath
- `nextPageButton`: 定位下一页按钮的 XPath
- 等等...

---

#### 配置加载流程

**插件启动流程**：

```
1. 插件启动
   ↓
2. 读取本地缓存的 XPath 配置
   ↓
3. 检查配置版本和过期时间
   ↓
4. 判断是否需要更新：
   - 配置过期（超过 24 小时）
   - 版本号落后
   - 本地无缓存
   ↓
5. 如需更新：
   - 后台从云端下载最新配置
   - 更新本地缓存
   - 如下载失败，使用缓存继续工作
   ↓
6. 使用配置中的 XPath 操作 DOM
```

**关键特性**：
- 非阻塞：后台更新，不影响用户使用
- 离线可用：本地缓存保证离线时仍可工作
- 自动更新：用户无感知，自动获取最新配置

---

#### 运行时错误处理流程

**当 DOM 操作失败时**：

```
1. 使用主 XPath 查找元素
   ↓
2. 如果找不到元素
   ↓
3. 尝试使用备用 XPath（降级方案）
   ↓
4. 如果仍然失败
   ↓
5. 记录失败信息（XPath、失败次数）
   ↓
6. 累计失败次数 ≥ 3 次
   ↓
7. 触发强制更新配置
   - 立即从云端下载最新配置
   - 提示用户："正在更新配置，请稍候..."
   ↓
8. 使用新配置重试操作
   ↓
9. 如果仍失败
   ↓
10. 提示用户：
    "Strava 页面已更新，配置正在更新中
     请稍后重试或联系支持"
```

**降级保障**：
- 第一层：主 XPath
- 第二层：备用 XPath
- 第三层：更新配置后重试
- 第四层：友好的错误提示

---

#### 配置更新策略

**定期检查**：
- 频率：每 24 小时检查一次
- 时机：插件启动时、后台空闲时
- 方式：对比云端和本地的版本号

**触发更新**：
- DOM 操作连续失败 3 次以上
- 用户手动触发（设置中的"检查更新"）
- 配置过期（超过 24 小时）

**更新过程**：
- 非阻塞：不影响当前操作
- 静默更新：后台完成，用户无感知
- 失败容错：下载失败仍使用缓存

---

#### 版本管理

**版本号规则**：`YYYY.MM.DD.NNN`
- 示例：2024.01.07.001
- YYYY.MM.DD：日期
- NNN：当天的版本号（从 001 开始）

**兼容性检测**：
- `minPluginVersion`: 配置文件要求的最低插件版本
- `stravaVersion`: 适配的 Strava 版本标识
- `testedDate`: 配置最后测试日期

**版本对比逻辑**：
```
本地版本 vs 云端版本
  ↓
如果云端版本更新
  ↓
检查 minPluginVersion
  ↓
如果当前插件版本 ≥ minPluginVersion
  ↓
下载并应用新配置
  ↓
否则
  ↓
提示用户更新插件
```

---

#### 核心优势

**1. 快速响应 Strava 改版**
- 修复时间从"天级"缩短到"小时级"
- 减少插件不可用的时间窗口
- 提升用户体验和满意度

**2. 降低维护成本**
- 无需修改代码、打包、提审
- 只需维护 JSON 配置文件
- 可以快速实验不同的 XPath 方案

**3. 提升系统稳定性**
- 备用选择器提供降级保障
- 本地缓存保证离线可用
- 智能重试机制提高成功率

**4. 增强灵活性**
- 可针对不同 Strava 版本提供不同配置
- 支持 A/B 测试不同的 XPath 方案
- 为未来功能扩展预留空间

---

#### 风险应对

**风险1：配置文件无法下载**
- 应对：使用本地缓存的上一版本配置
- 保障：离线模式下仍可正常工作

**风险2：新配置文件有错误**
- 应对：插件启动时验证关键 XPath
- 保障：发现错误自动回滚到上一版本

**风险3：Strava 大幅改版**
- 应对：提供多个备用 XPath 选择器
- 保障：至少保证核心功能可用

**风险4：网络不稳定导致更新失败**
- 应对：多次重试机制（最多 3 次）
- 保障：使用缓存继续工作

---

#### V1.0 实现范围

**必须实现**：
- ✅ XPath 配置文件（JSON 格式）
- ✅ 配置加载机制（启动时加载）
- ✅ 本地缓存机制（Chrome Storage）
- ✅ 云端配置下载（从你的网站）
- ✅ 版本检测和对比
- ✅ 基础的错误处理和降级
- ✅ DOM 操作封装（统一使用配置的 XPath）

**可选实现（V2.0）**：
- ⚠️ 多版本配置并存（针对不同 Strava 版本）
- ⚠️ A/B 测试不同 XPath 方案
- ⚠️ 自动检测 Strava 版本并选择对应配置
- ⚠️ 配置错误的自动回滚机制

---

#### 用户体验

**对用户完全透明**：
- 用户无需了解 XPath 是什么
- 无需手动更新配置
- 无需关心 Strava 是否改版
- 一切都在后台自动完成

**异常情况提示**：
- 正常情况：完全静默，无任何提示
- 配置更新中：简单提示"正在更新配置..."
- 更新失败：提示"页面结构已变化，请稍后重试"
- 降级使用：继续工作，无提示

**设置界面（可选）**：
- 查看当前配置版本
- 手动检查更新
- 查看配置更新历史
- 切换到安全模式（使用备用选择器）

---

### 技术实现范围限制 ✅ 已确定

#### 核心限制：仅使用快捷更新（Quick Edit）功能

**实现方式**：
```
Strava 训练日志页面
  ↓
每个活动行旁边的"Quick Edit"按钮
  ↓
点击后在当前行展开简化的编辑表单
  ↓
插件操作这个快捷表单的字段
  ↓
提交保存
```

**明确不做**：
- ❌ 不打开活动详情页
- ❌ 不操作 iframe 内的编辑表单
- ❌ 不使用详情页的完整编辑功能
- ❌ 不跳转到单独的编辑页面

---

#### 可编辑字段范围

**受限于快捷更新暴露的字段**：

✅ **支持的字段**（快捷更新提供）：
- 运动类型（Activity Type）
- 骑行类型（Ride Type）
- 跑步类型（Workout Type）
- 自行车装备（Bike）
- 跑鞋装备（Shoes）
- 隐私设置（Privacy）

❌ **不支持的字段**（快捷更新未暴露）：
- 活动名称（Title）
- 活动描述（Description）
- 标签（Tags）
- 照片管理
- 开始时间
- GPS 轨迹数据
- 心率、功率等传感器数据
- 其他高级字段

---

#### 限制的原因

**1. 技术实现简单可靠**
- 快捷更新是 Strava 原生功能
- DOM 结构相对稳定
- 不需要处理复杂的页面跳转
- 避免 iframe 跨域问题

**2. 批量操作效率高**
- 不需要为每个活动打开详情页
- 在列表页直接批量编辑
- 速度快，用户等待时间短
- 减少 API 请求次数

**3. 满足核心需求**
- 装备管理（鞋、车）是最常见需求 ✅
- 类型标记（骑行/跑步类型）是常见需求 ✅
- 隐私设置是常见需求 ✅
- 这三类已覆盖 90% 的批量编辑场景

**4. 降低维护成本**
- 不需要适配详情页的复杂表单
- XPath 配置更简单
- 减少因 Strava 改版导致的维护工作

---

#### 与产品定位的对应

**产品定位**：专注于装备管理和隐私批量操作

✅ **在范围内**（已规划的场景）：
- 🔒 调整隐私设置
- 👟 更新跑鞋记录
- 🚴 更新骑行装备
- 🏃 修改运动类型

❌ **不在范围内**（快捷更新不支持）：
- 批量修改活动标题
- 批量修改活动描述
- 批量添加/删除标签
- 批量管理照片
- 批量调整开始时间
- 等复杂编辑需求

**说明**：
- 这些限制是技术实现层面的主动选择
- 不是技术能力不足，而是为了产品定位清晰
- 未来如有需要，可作为 V2.0 功能考虑

---

#### 对竞品的影响

**竞品分析**（updateActivities.js）：
- 竞品也只使用快捷更新功能
- 竞品也不操作详情页
- 我们的限制与竞品保持一致
- 在同样的技术约束下，我们提供更好的用户体验

---

## 📊 场景复杂度对比

```
场景类型          UI模式        规则数量      适用场景
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⭐ 简单           单次配置      1条规则       • 调整隐私
                                            • 修改运动类型

⭐⭐ 中等         单次配置      1条规则       • 简单装备更换
                或规则列表    或多条规则     • 单一时间段更新

⭐⭐⭐ 复杂       规则列表      多条规则       • 跑鞋轮换记录
                (推荐)                      • 复杂装备更换
                                            • 多时段+多条件
```

---

## 🎯 关键设计决策

### 1. 场景化入口 vs 传统字段选择

**✅ 采用场景化入口**

理由：
- 更符合用户思维（"我要做什么"而非"我要改什么字段"）
- 降低学习成本（零学习，选场景即可）
- 避免不合理组合（如给骑行活动设置跑鞋）
- 可针对场景优化UI和交互

### 2. 单次配置 vs 规则列表

**✅ 根据场景复杂度动态选择**

- 简单场景（如调整隐私）→ 单次配置模式
- 复杂场景（如更新跑鞋）→ 规则列表模式
- 由场景配置的 `ui.mode` 字段决定

### 3. 规则冲突处理

**✅ 后执行覆盖 + 预览高亮冲突**

执行逻辑：
- 按规则顺序执行
- 如果活动匹配多条规则，后面的规则覆盖前面的
- 预览时明确提示冲突的活动

用户控制：
- 可调整规则顺序（上移/下移）
- 可编辑规则避免冲突
- 可删除不需要的规则

### 4. 配置化 vs 硬编码

**✅ 场景定义完全配置化**

优势：
- 新增场景无需发版
- 快速响应用户需求
- 支持配置迭代和A/B测试
- 为未来社区贡献场景留下扩展空间

限制：
- V1.0 配置源为你的个人网站
- 不考虑安全验证（仅自用）
- 不开放给其他用户

### 5. 预览功能

**✅ 可选预览，支持跳过**

- 预览不写入数据，只模拟
- 显示所有匹配的活动和修改对比
- 按规则分组显示结果
- 高亮冲突的活动
- 用户可选择跳过直接执行（提速）

### 6. 暂停/继续机制

**✅ 支持暂停，不支持撤销**

- 暂停后对话框保持打开
- 继续从当前位置执行
- 停止后已修改的数据保留
- V1.0 不实现撤销功能

---

## 🤔 待讨论事项

### 1. 规则模板保存功能

**问题**：用户配置好一套规则后，是否支持保存为模板？

**方案A**：支持保存到本地
```
优点：下次使用时快速加载
缺点：跨设备不同步
```

**方案B**：支持保存到云端（你的网站）
```
优点：跨设备同步
缺点：需要实现云端存储
```

**方案C**：仅支持导出/导入JSON
```
优点：实现简单，用户自行管理
缺点：操作略复杂
```

**建议**：V1.0 先实现方案C，后续考虑方案A或B

---

### 2. 规则执行的事务性

**问题**：如果规则2执行失败，是否继续执行规则3？

**方案A**：继续执行（推荐）
```
规则1 → 成功 ✅
规则2 → 失败 ❌ (部分活动失败)
规则3 → 继续执行 ✅

优点：最大化执行成功的规则
缺点：可能导致数据不一致
```

**方案B**：全部回滚
```
规则1 → 成功 ✅
规则2 → 失败 ❌
→ 回滚规则1

优点：数据一致性强
缺点：实现复杂，用户体验差
```

**建议**：V1.0 采用方案A，在结果中明确显示每条规则的执行情况

---

### 3. 活动数量限制 ✅ 已确定

**决策**：不限制数量，全部执行

**智能暂停机制**：
- 检测连续失败（如连续5个活动更新失败）
- 检测 API 返回异常（如 429 限流、5xx 错误等）
- 触发暂停时：
  - 保存当前执行的规则索引
  - 保存当前页面位置
  - 保存已处理的活动列表
  
**恢复执行**：
- 下次启动时检测是否有未完成的任务
- 提示用户是否继续上次的执行
- 从保存的进度继续执行

**优点**：
- ✅ 满足大批量需求
- ✅ 智能容错，避免无效重试
- ✅ 支持断点续传

---

### 4. 场景配置的UI编辑器 ✅ 已确定

**决策**：纯手工编辑 JSON

**理由**：
- ✅ 配置仅自己使用，数量有限
- ✅ 实现成本为零
- ✅ 灵活性最高
- ✅ 使用 TypeScript 类型定义辅助编辑

---

### 5. 错误重试机制 ✅ 已确定

**决策**：自动重试（最多3次）

**实现细节**：
- 首次失败：等待 1 秒后重试
- 第二次失败：等待 2 秒后重试
- 第三次失败：等待 4 秒后重试
- 仍失败：记录失败原因，继续处理下一个活动

**重试条件**：
- ✅ 网络错误
- ✅ 超时错误
- ✅ 5xx 服务器错误
- ❌ 4xx 客户端错误（不重试，直接记录失败）

**优点**：
- ✅ 提高成功率
- ✅ 应对网络抖动
- ✅ 不打断自动化流程

---

### 6. 页面加载检测的可靠性 ✅ 已确定

**决策**：两种方式结合

**实现细节**：

**主要方式：监控 API 接口**
```javascript
// 监听 Strava 训练日志列表的 API 请求
// 典型接口：/athlete/training_activities
// 等待接口返回 + 短暂延迟（如 500ms）
```

**兜底方式：检测 DOM 元素**
```javascript
// 检测高亮页码元素的变化
// 检测活动列表容器的更新
// 设置最长等待时间（如 10 秒）
```

**综合判断**：
1. 优先使用 API 监控（最可靠）
2. 如果 API 监控失败，使用 DOM 检测
3. 两种方式都超时，记录警告并继续

**优点**：
- ✅ 高可靠性
- ✅ 有容错机制
- ✅ 适应 Strava 的变化

---

## ✅ 流程设计总结

### 核心创新点

1. **场景化入口** - 用户只需选择"我要做什么"，零学习成本
2. **配置化场景** - 新增场景无需发版，快速迭代
3. **规则列表模式** - 支持复杂的多规则组合（如跑鞋轮换记录）
4. **底层通用引擎** - 一次开发，支持所有场景
5. **预览和冲突检测** - 安全可控，避免误操作

### 实现优先级

```
P0 - 必须实现
├── 场景化入口系统
├── 配置加载和缓存
├── 通用规则引擎
├── 简单场景（隐私、运动类型）
└── 预览功能

P1 - 应该实现
├── 复杂场景（跑鞋、骑行装备）
├── 规则列表UI
├── 冲突检测和提示
└── 进度显示和暂停

P2 - 可以实现
├── 规则模板保存
└── 规则复制功能
```

---

**流程设计已完成！✅** 准备进入下一阶段：UI 设计和技术实现 📝


